# Copy to .env for local secrets. Never commit real credentials.
DATABASE_PATH=data/job_agent.sqlite3
LOG_LEVEL=INFO

# Optional. Keep the real key only in .env or your shell environment.
OPENAI_API_KEY=
